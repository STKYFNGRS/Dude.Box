'use client';

// Placeholder file - Web3 functionality removed
export const useWeb3 = () => {
  return {
    isConnected: false,
    address: null,
    connect: () => {},
    disconnect: () => {},
  };
};